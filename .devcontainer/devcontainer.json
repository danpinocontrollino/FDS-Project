{
  "name": "FDS Mental Health Profiling",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
        "python.terminal.activateEnvironment": true
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter"
      ]
    }
  },
  
  "postCreateCommand": "bash -c 'if [ ! -d .venv ]; then python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt; fi'",
  
  "postStartCommand": "echo '✅ FDS Project Environment Ready! Virtual environment will activate automatically in new terminals.'",
  
  "features": {
    "ghcr.io/devcontainers/features/git:1": {},
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.12"
    }
  }
}
